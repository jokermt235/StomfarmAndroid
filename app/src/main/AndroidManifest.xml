<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="devel.exesoft.com.accshop">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera2" />
    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".controller.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:noHistory="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.RemainActivity"></activity>
        <activity android:name=".view.MannualActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.HomeActivity" />
        <activity android:name=".view.ClientsActivity" />
        <activity android:name=".view.DebtsActivity" />
        <activity android:name=".view.PartnerActivity" />
        <activity android:name=".view.NewPartnerActivity" />
        <activity android:name=".view.OrdersActivity" />
        <activity android:name=".view.NewOrderActivtiy" />
        <activity android:name=".view.NewOrderAddItemManuallyActivity" />
        <activity android:name=".view.SimpleScannerActivity" />
        <activity android:name=".view.StorageActivity" />
    </application>

</manifest>